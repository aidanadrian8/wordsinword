import { createStore } from 'vuex'
import sessionsManager from "@/store/modules/session_manager.js"

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    sessionsManager
  }
})
